name: Test Kind CI

on:
  push:
    branches: [ main ]

jobs:
  test-kind:
    runs-on: [self-hosted, kind]

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build test Docker image
        run: |
          docker build -t howco/devops-dir:test-action-foobarbaz ./api-go

      - name: Push test Docker image
        run: |
          docker push howco/devops-dir:test-action-foobarbaz

      - name: Set kubectl context to Kind
        run: |
          kubectl config use-context kind-kind

      - name: Deploy test pod
        run: |
          kubectl create deployment test-action-nginx --image=howco/devops-dir:test-action-foobarbaz || true
          kubectl rollout status deployment/test-action-nginx --timeout=60s

      - name: Cleanup
        run: |
          kubectl delete deployment test-nginx || true
